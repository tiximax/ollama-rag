{
  "name": "ollama-rag-e2e",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:light": "playwright test --grep-invert @heavy",
    "test:e2e:headed": "playwright test --headed",
    "test:e2e:debug": "playwright test --debug",
    "test:e2e:ci:light": "playwright test --config=playwright.ci.config.js --grep-invert @heavy",
    "smoke:tunnel:native": "powershell -NoProfile -ExecutionPolicy Bypass -File .\\scripts\\smoke\\cloudflared_smoke.ps1",
    "smoke:tunnel:docker": "powershell -NoProfile -ExecutionPolicy Bypass -File .\\scripts\\smoke\\docker_tunnel_smoke.ps1",
    "playwright:install": "playwright install",
    "playwright:install-deps": "playwright install --with-deps"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
